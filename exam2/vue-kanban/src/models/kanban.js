export default {
    methods : {
        /** 작업 추가 */
        $addWork(data) {
            console.log(data);
        },
        /** 작업 수정 */
        $editWork(data) {
            console.log(data);
        },
        /** 작업 삭제 */
        $deleteWork(idx) {
            console.log(idx);
        },
        /** 작업 목록  */
        $getList(status) {
            console.log(status);
        }
    }
}